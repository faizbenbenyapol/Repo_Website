-- แก้บั๊กของ v0.7.0: engines.treeSitter/pattern นับเฉพาะไฟล์ที่พาร์สใหม่ในรอบนั้น
-- พอไฟล์ทั้งหมดไม่เปลี่ยนเลย (ใช้ผลเดิมซ้ำทั้งก้อน) ยอดรวมเลยเหลือ 0 ทั้งที่ไฟล์ถูกพาร์สจริงมาก่อนแล้ว
-- เก็บเครื่องมือที่ใช้พาร์สไว้รายไฟล์ เพื่อให้รอบที่ใช้ผลเดิมซ้ำยังรวมยอดของไฟล์ที่ข้ามไปได้ถูกต้อง

alter table files add column if not exists engine text;
