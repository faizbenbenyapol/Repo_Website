# บันทึกการเปลี่ยนแปลง

ไฟล์นี้ประกอบขึ้นอัตโนมัติจาก `docs/changelog/*.md` ด้วยคำสั่ง `make changelog` — อย่าแก้ด้วยมือ

## v0.8.0 — เข้าถึงได้จริง อ่านได้บนมือถือ ไม่มีอนิเมชันกวนใจ

วันที่ 2026-09-08

### เพิ่ม

- ชุดทดสอบอัตโนมัติด้วย axe-core ตรวจ WCAG 2.1 AA (`wcag2a`/`wcag2aa`/`wcag21a`/`wcag21aa`) ทุกหน้าหลัก ทั้งธีมสว่างและธีมมืด — หน้าแรก หน้าเข้าสู่ระบบ หน้าสมัครสมาชิก หน้าประวัติเวอร์ชัน หน้าแผนที่โค้ด หน้าสุขภาพโค้ด และหน้าถาม–ตอบ รวม 11 เทสต์
- ชุดทดสอบอัตโนมัติตรวจว่าไม่มีหน้าไหนล้นแนวนอนที่ความกว้างมือถือ (375px) ครอบคลุมหน้าหลักชุดเดียวกัน

### แก้

- สีตัวหนังสือรอง (`--color-muted`, `--color-faint`) และสีทองแดง (`--color-brass`) เข้มไม่พอจนคอนทราสต์ กับพื้นหลังต่ำกว่า 4.5:1 จริงตอนเรนเดอร์ — ค่าที่คำนวณตามสูตรทฤษฎีผ่าน แต่การเรนเดอร์ตัวอักษรเล็กจริง ในเบราว์เซอร์มี anti-aliasing ที่ลดคอนทราสต์ลงจากค่าทฤษฎีอีกชั้นหนึ่ง จึงต้องเผื่อขอบเพิ่มทั้งสองธีม
- ปุ่มหลัก (bg-accent + ตัวหนังสือขาว) ในธีมมืดอ่านแทบไม่ออก (วัดได้ 2.29:1) เพราะสีเน้นของธีมมืดต้อง สว่างพอให้ตัวมันเองอ่านออกบนพื้นมืด แต่พาสเทลสว่างขนาดนั้นใส่ตัวหนังสือขาวทับไม่ผ่านเกณฑ์ — เพิ่มโทเค็น `--color-on-accent` แยกต่างหาก (ขาวในธีมสว่าง เข้มในธีมมืด) ให้ทุกปุ่มสีเน้นใช้แทน `text-white` เดิม
- แถบหัวเว็บ (โลโก้ + เมนู + ชิปเซสชัน) ล้นออกนอกจอที่ความกว้างมือถือ 375px (ยาวเกิน 21px) — ให้เมนูตัด บรรทัดได้ตามพื้นที่ที่เหลือ และซ่อนลิงก์ "ต้นทางแนวคิด" ซึ่งสำคัญน้อยสุดไว้บนจอกว้างขึ้นเท่านั้น
- axe รายงานคอนทราสต์ผิดพลาดที่หน้าถาม–ตอบเป็นบางครั้ง — สาเหตุคือสแกนไปกลางอนิเมชันจางเข้า (`.rise`) ขณะองค์ประกอบยังโปร่งแสงไม่เต็มที่ ไม่ใช่ปัญหาสีจริง แก้ที่ชุดทดสอบให้ปิดอนิเมชันก่อนสแกนเสมอ
- ยอดรวม `engines.treeSitter`/`engines.pattern` ของผลวิเคราะห์รายงานเป็น 0 เมื่อทุกไฟล์ในรอบนั้นไม่มี การเปลี่ยนแปลงเลยและใช้ผลจากรอบก่อนซ้ำทั้งหมด (การวิเคราะห์ซ้ำแบบ incremental ของ v0.7.0) ทั้งที่ทุกไฟล์ ผ่านการพาร์สจริงมาก่อนแล้ว — ตอนนี้เก็บเครื่องมือที่พาร์สแต่ละไฟล์ไว้รายไฟล์ (คอลัมน์ `files.engine`) เพื่อให้รอบที่ใช้ผลเดิมซ้ำยังรวมยอดจากไฟล์ที่ข้ามไปได้ถูกต้อง

### ปรับ

- ปุ่มและลิงก์ที่มีสถานะโฟกัสหรือ hover ผ่านการตรวจ axe ครบทุกหน้า ไม่ใช่แค่ไม่มีปัญหาระดับ critical เหมือนเกณฑ์ผ่านขั้นต่ำของ PLAN.md — เป้าคือไม่มีปัญหาการเข้าถึงเลยสักข้อในทุกหน้าที่ทดสอบ

### หมายเหตุอัปเกรด

- เกณฑ์ "60fps" ของ PLAN.md ตรวจแบบอ่านโค้ดเป็นหลัก ไม่ได้วัดเฟรมจริงด้วยเครื่องมือโปรไฟล์: อนิเมชันที่มี อยู่ทั้งหมดในเว็บ (`.rise`, `animate-pulse`, การเปลี่ยนสี/ทรานส์ฟอร์มตอน hover) เป็น CSS ล้วนและแตะแค่ `opacity`/`transform` ซึ่งเบราว์เซอร์รันบน compositor แยกจาก main thread ได้โดยไม่ชนเฟรมอยู่แล้ว ไม่มี `requestAnimationFrame` หรือลูปแอนิเมชันที่เขียนเอง กราฟความสัมพันธ์ที่วาดด้วย sigma.js/WebGL ยังไม่ได้ถูกวัดประสิทธิภาพกับ repo ขนาดใหญ่มากเป็นพิเศษโดยเฉพาะ
- คอลัมน์ `files.engine` ใหม่เป็น `null` สำหรับผลวิเคราะห์เก่าที่มีอยู่ก่อนอัปเกรดรุ่นนี้ — รอบวิเคราะห์ซ้ำ ของ repo เหล่านั้นจะยังไม่รวมยอด engines ของไฟล์ที่ใช้ผลเดิมซ้ำจนกว่าจะมีการวิเคราะห์แบบเต็มรูปแบบอีกครั้ง ไม่กระทบข้อมูลอื่นใด เป็นแค่ตัวเลขสรุปที่จะถูกต้องเองเมื่อวิเคราะห์ใหม่

## v0.7.0 — ลิงก์ถาวร รายงาน และวิเคราะห์ซ้ำที่เร็วขึ้น

วันที่ 2026-09-08

### เพิ่ม

- ลิงก์ถาวรที่ `/r/[host]/[owner]/[name]` — เปิดได้เสมอโดยไม่ต้องจำรหัสงานวิเคราะห์ที่เป็น UUID พาไปผลล่าสุดของ repo นั้นทันทีถ้ามีอยู่แล้ว หรือสั่งวิเคราะห์ใหม่ให้เองถ้ายังไม่เคยมี หน้าแผนที่โค้ดแสดงลิงก์นี้ไว้ให้คัดลอกไปแชร์ต่อได้
- ส่งออกรายงานภาษาไทยทั้งชุดเป็น Markdown ไฟล์เดียวที่ `GET /api/analyses/:id/export/report.md` รวมทั้งคะแนนสุขภาพ จุดร้อน วงจรพึ่งพา ข้อสังเกตด้านความปลอดภัย และชั้นความเข้าใจที่สมาชิกสั่งไว้ (สรุปภาพรวม คำอธิบายรายโมดูล เส้นทางอ่านโค้ด) เป็นเอกสารเดียวที่ส่งต่อให้ทีมได้เลย
- วิเคราะห์ repo เดิมซ้ำเร็วขึ้นมาก — ไฟล์ที่เนื้อหาไม่เปลี่ยนจากรอบก่อนจะไม่ถูกอ่านหรือพาร์สด้วย tree-sitter ซ้ำเลย ใช้สัญลักษณ์ เส้นเชื่อม ข้อสังเกตด้านความปลอดภัย และแพ็กเกจภายนอกจากรอบก่อน แทนทั้งหมด ยิ่ง repo ใหญ่และแก้แค่ไม่กี่ไฟล์ รอบสองก็ยิ่งเร็วขึ้นมาก
- เก็บแพ็กเกจภายนอกที่แต่ละไฟล์เรียกใช้แยกไว้ต่างหาก เพื่อให้สรุปรวมยังถูกต้องครบแม้ไฟล์ส่วนใหญ่ จะถูกข้ามการพาร์สไปในรอบวิเคราะห์ซ้ำ

### แก้

- ตารางฟีเจอร์ยังไม่เคยมีลิงก์ถาวรอยู่ในรายการเลยตั้งแต่ต้น ทั้งที่ PLAN.md วางแผนไว้ตั้งแต่ v0.1.0

### หมายเหตุอัปเกรด

- รายงานส่งออกยังไม่มีรูปแบบ PDF — `pdfkit` ที่เบาที่สุดไม่มีฟอนต์ภาษาไทย ส่วนทางที่ถูกต้อง (เรนเดอร์ HTML ด้วยเบราว์เซอร์จริง) ต้องเพิ่ม Chromium เข้าอิมเมจ API ซึ่งเป็นการตัดสินใจ ด้านโครงสร้างพื้นฐานที่ยังรอเจ้าของงานเลือกทาง — ใช้ Markdown ไปก่อนในระหว่างนี้

## v0.6.0 — ถาม–ตอบและเส้นทางอ่านโค้ด

วันที่ 2026-09-08

### เพิ่ม

- ถาม–ตอบกับ repo เป็นภาษาไทยที่หน้า `/a/[id]/chat` — พิมพ์คำถาม ได้คำตอบพร้อมอ้างอิงไฟล์และบรรทัดจริง ที่ผ่านการตรวจแล้วว่ามีอยู่จริงเช่นเดียวกับชั้นความเข้าใจของ v0.5.0
- ค้นคืนบริบทด้วย `pg_trgm` — จับคู่คำถามกับชื่อไฟล์ ชื่อฟังก์ชัน และข้อความสรุปที่ทำไว้แล้วจาก v0.5.0 เลือกทางนี้แทนเวกเตอร์ฝังเพราะสิ่งที่ต้องค้นเป็นข้อความสั้นที่ตรงตัวอักษรได้ระดับหนึ่งอยู่แล้ว ไม่ต้องเพิ่มคอนเทนเนอร์รันโมเดลฝังเวกเตอร์แยก (ดู ADR 0003)
- เมื่อค้นไม่เจอหลักฐานที่ตรงกับคำถามเลย ระบบตอบตรง ๆ ว่าไม่พบ แทนการเดาจากความรู้ทั่วไปเกี่ยวกับโค้ด ที่หน้าตาคล้ายกัน — คำตอบทุกข้อจึงมีอ้างอิงบรรทัดจริงกำกับ หรือไม่ก็ไม่มีคำตอบเลย ไม่มีข้อความลอย ๆ อยู่ตรงกลาง
- ถามต่อเนื่องได้ — คำถามใหม่เห็นบทสนทนาสามข้อล่าสุดของตัวเอง จึงอ้างถึง "มัน" หรือ "ไฟล์นั้น" ต่อจากคำถามก่อนได้
- เส้นทางอ่านโค้ดสำหรับคนใหม่ที่หน้ารายงาน — เรียงไฟล์ 7–15 ขั้นจากไฟล์พื้นฐานที่ไฟล์อื่นเรียกใช้ ไปจนถึงจุดเริ่มโปรแกรม ด้วย topological sort บนกราฟการนำเข้าจริง ไม่ใช่ให้โมเดลเดาลำดับ โมเดลมีหน้าที่แค่อธิบายว่าทำไมแต่ละขั้นถึงควรอ่านตอนนั้นและควรสังเกตอะไร
- ไฟล์ที่พึ่งพากันเป็นวงจร (ไม่มีลำดับที่ถูกต้องแท้จริง) ยังปรากฏในเส้นทางอ่านโค้ด ไม่ใช่หายไปเงียบ ๆ
- เส้นทาง API ใหม่สี่เส้น: ถามคำถาม, ดูประวัติบทสนทนา, สร้างเส้นทางอ่านโค้ด, ดูเส้นทางล่าสุดที่เคยสร้างไว้
- เพดานจำนวนครั้งต่อชั่วโมงแยกกันสำหรับถาม–ตอบและเส้นทางอ่านโค้ด เช่นเดียวกับงานสรุปของ v0.5.0 เพราะทั้งสองเรียกโมเดลด้วยกุญแจของผู้ใช้เอง

### แก้

- ตารางฟีเจอร์ยังบอกว่าถาม–ตอบและเส้นทางอ่านโค้ดเป็นของที่ยังไม่มี ทั้งที่ปล่อยไปแล้วในรุ่นนี้

### ปรับ

- ดึงตัวตรวจ "กุญแจผิดต้องหยุดทันที" ของชั้นความเข้าใจออกมาเป็นฟังก์ชันกลางใน `@repolens/ai` เพราะตอนนี้มีสามฟีเจอร์ (สรุป, ถาม–ตอบ, เส้นทางอ่านโค้ด) ที่ต้องเช็กเรื่องเดียวกัน

## v0.5.0 — เข้าใจเป็นภาษาไทย

วันที่ 2026-09-08

### เพิ่ม

- ระบบสมาชิกด้วยบัญชีในระบบเอง — สมัคร เข้าสู่ระบบ และออกจากระบบ รหัสผ่านแฮชด้วย scrypt พร้อมเกลือของตัวเอง และ session เป็นโทเค็นสุ่มในคุกกี้ที่สคริปต์อ่านไม่ได้ โดยฐานข้อมูลเก็บไว้แค่ค่าที่ย่อแล้ว ถ้าฐานข้อมูลหลุดก็เอาไปสวมรอยไม่ได้
- ช่องใส่ API key ของ Claude ที่หน้า `/account` — ตรวจกับ Anthropic ให้ก่อนรับเก็บ จึงรู้ตั้งแต่ตอนใส่ว่าพิมพ์ผิดหรือเปล่า แทนที่จะไปรู้ตอนสั่งสรุปแล้วรอฟรีไปสองนาที
- กุญแจของสมาชิกอยู่ในหน่วยความจำเท่านั้น เข้ารหัสด้วย AES-256-GCM และหมดอายุเองใน 12 ชั่วโมง ไม่เคยถูกเขียนลงฐานข้อมูล ไม่เคยลง log และไม่เคยถูกส่งกลับไปหน้าเว็บ — สิ่งที่หน้าเว็บรู้ได้มากที่สุดคือสี่ตัวท้าย
- ชั้นความเข้าใจแบบสามชั้น — สรุปรายไฟล์ รวมเป็นภาพของแต่ละโมดูล แล้วสรุปภาพรวมทั้ง repo ตอบให้ได้ว่าโปรเจกต์นี้ทำอะไร สร้างด้วยอะไร เริ่มอ่านจากไฟล์ไหน รันอย่างไร และมีอะไรควรระวัง
- ทุกประโยคของคำอธิบายต้องชี้บรรทัดจริงในโค้ดได้ ข้อที่ชี้ไม่ได้ถูกตัดทิ้งก่อนถึงหน้าจอ ไม่ใช่แสดงแบบไม่มีที่มา เพราะคำอธิบายที่ตรวจย้อนกลับไม่ได้และฟังดูน่าเชื่อ อันตรายกว่าการไม่มีคำอธิบายเลย
- ไฟล์ทุกไฟล์มีคำอธิบายเสมอ ไฟล์ที่เกินเพดานการเรียกโมเดลหรืออ่านเนื้อหาไม่ได้ จะได้คำอธิบายที่ระบบเขียนเองจากโครงสร้างที่วัดได้ และติดป้ายบอกไว้ว่ามาจากไหน
- แผงรายละเอียดไฟล์ในแผนที่โค้ดแสดงคำอธิบายของไฟล์นั้น พร้อมป้ายอ้างอิงที่คลิกกระโดดต่อได้
- หน้ารายงานเพิ่มส่วนสรุปภาษาไทยของทั้ง repo และคำอธิบายรายโมดูล
- ปุ่มสั่งสรุปพร้อมแถบความคืบหน้าที่เดินตามขั้นจริงผ่าน SSE และบอกจำนวนไฟล์ที่สรุปไปแล้ว
- คิวงานที่สองแยกจากคิววิเคราะห์ คนที่แค่อยากดูกราฟจึงไม่ต้องรอคิวของคนที่สั่งสรุปทั้ง repo
- แพ็กเกจ `@repolens/ai` — ตัวเรียก Claude API ที่เขียนบน fetch เอง ไม่มี dependency ภายนอกเพิ่ม พร้อมตัวแกะ JSON ที่ทนคำตอบเพี้ยน และตัวตรวจอ้างอิงที่ตัดข้อที่ยืนยันไม่ได้ออก
- เพดานจำนวนไฟล์ต่อการสรุปหนึ่งรอบ และเพดานจำนวนครั้งที่สั่งสรุปได้ต่อชั่วโมง เพราะค่าเรียกใช้โมเดลตกกับกุญแจของผู้ใช้ทุกบาท จำนวนครั้งจึงต้องคาดเดาได้
- นับโทเค็นที่ใช้ไปทั้งงานแล้วแสดงให้เจ้าของกุญแจเห็นว่ารอบนี้จ่ายไปเท่าไร
- `git clone` รับหมายเลขคอมมิตได้แล้ว งานสรุปจึงกลับไปอ่านโค้ดของคอมมิตเดียวกับที่วิเคราะห์ไว้ได้

### แก้

- ตารางฟีเจอร์ยังบอกว่าคะแนนสุขภาพ รัศมีผลกระทบ และการส่งออก JSON เป็นของที่ยังไม่มี ทั้งที่ปล่อยไปตั้งแต่ v0.4.0 — หน้าแรกจึงโฆษณาน้อยกว่าที่ทำได้จริงมาตลอดหนึ่งรุ่น
- ตัวส่งต่อคำขอของหน้าเว็บรวมคุกกี้หลายใบเป็นค่าเดียว ซึ่งเบราว์เซอร์อ่านไม่ออก ตอนนี้ส่งต่อทีละใบตามที่ควรเป็น
- ข้อความบนหน้าแรกยังบอกว่ารุ่นปัจจุบันคือ v0.2.0 ทั้งที่ผ่านมาสองรุ่นแล้ว
- เพดานการสั่งวิเคราะห์หักโควตาให้คำขอที่จบลงด้วยการคืนผลที่แคชไว้ด้วย ทั้งที่คำขอแบบนั้นไม่ได้ทำให้เครื่องโคลนอะไรเลย — เพดานนี้มีไว้กันการโคลน ไม่ใช่กันการอ่านผลเดิม
- migration ที่ถูกเรียกพร้อมกันจากหลายกระบวนการชนกันเองตอนฐานข้อมูลยังว่าง เพราะล็อกถูกจับคนละการเชื่อมต่อกับตัวที่รัน migration จริง ล็อกนั้นจึงไม่ได้กันอะไรเลยมาตั้งแต่ v0.2.0 บนเซิร์ฟเวอร์จริงคือกรณีที่ API กับ worker ขึ้นพร้อมกันครั้งแรก แล้วตัวที่แพ้เปิดไม่ขึ้น

### ปรับ

- Redis ทำงานในหน่วยความจำล้วนทั้งสามสภาพแวดล้อม เพื่อให้คำว่า "กุญแจไม่ลงดิสก์" เป็นจริง ไม่ใช่แค่ความตั้งใจ — แลกกับการที่งานค้างคิวจะหายเมื่อรีสตาร์ต ซึ่งสั่งใหม่ได้
- คุกกี้และหัวข้อ `set-cookie` ถูกกลบใน log ด้วย เพราะโทเค็น session สวมรอยเป็นเจ้าของบัญชีได้ทันที
- แยกตัวจำกัดอัตราออกมาเป็นโมดูลของตัวเอง เพราะตอนนี้มีสามที่ที่ต้องใช้กติกาเดียวกัน
- แยก worker เป็นไฟล์ต่อชนิดงาน จากเดิมที่รวมอยู่ไฟล์เดียว

### หมายเหตุอัปเกรด

- ต้องตั้ง `SESSION_SECRET` ก่อนเปิดบริการ สร้างด้วย `openssl rand -hex 32` API จะไม่ยอมเปิดบนเซิร์ฟเวอร์จริงถ้าไม่มีค่านี้ และถ้าเปลี่ยนค่า กุญแจที่ผู้ใช้ผูกไว้จะถอดไม่ออกทั้งหมด
- volume `redisdata` ไม่ถูกใช้แล้ว ลบทิ้งได้หลังอัปเดตด้วย `docker volume rm repolens_redisdata`

## v0.4.0 — ตัวชี้วัด

วันที่ 2026-09-07

### เพิ่ม

- คะแนนสุขภาพโค้ด A–F ที่เริ่มจากเต็มร้อยแล้วหักตามสิ่งที่วัดได้จริง ทุกข้อที่หักบอกได้ว่าหักเพราะอะไรและเท่าไร — ตัวเลขที่อธิบายที่มาไม่ได้ ใช้ตัดสินใจไม่ได้
- หาวงจรพึ่งพาด้วยอัลกอริทึมของ Tarjan เขียนแบบวนซ้ำ จึงไม่ล้นแม้กราฟจะลึกหลักหมื่น
- รัศมีผลกระทบรายไฟล์ — ถ้าแก้ไฟล์นี้ ไฟล์อื่นได้รับผลกี่ไฟล์ นับรวมผลทางอ้อมที่ไล่ต่อกันเป็นทอด ๆ
- หาโค้ดที่ไม่มีใครเรียกใช้ โดยยกเว้นจุดเริ่มโปรแกรม ไฟล์ทดสอบ ไฟล์ตั้งค่า และสคริปต์
- อ่านประวัติ git ย้อนหลังสองร้อยคอมมิต เพื่อบอกจุดร้อน (ไฟล์ที่แก้บ่อย ใหญ่ และมีคนพึ่งพาเยอะ) และบอกว่าควรถามใครเรื่องไฟล์นั้น
- สแกนความปลอดภัยเบื้องต้นเก้ากฎ — กุญแจที่เขียนตรงในโค้ด, กุญแจส่วนตัว, AWS key, การต่อสตริง SQL, eval, การส่งคำสั่งเข้า shell, การปิดตรวจใบรับรอง TLS, ตัวสุ่มที่ไม่เหมาะกับงานความปลอดภัย และคำสั่งดีบักที่ค้างอยู่ โดยยกเว้นไฟล์ทดสอบและเอกสารในกฎที่มักเจอค่าตัวอย่าง
- กลบค่าที่ดูเหมือนความลับก่อนเก็บลงฐานข้อมูลและก่อนแสดงบนหน้าเว็บทุกครั้ง
- โหมดลงสีที่สี่ของกราฟ — ตามรัศมีผลกระทบ
- แผงรายละเอียดไฟล์บอกรัศมีผลกระทบ จำนวนครั้งที่ถูกแก้ และผู้เขียนที่แตะไฟล์นี้บ่อยที่สุด
- หน้าสุขภาพโค้ดที่ `/a/[id]/report` — เกรด รายละเอียดการหักคะแนน จุดร้อน รัศมีผลกระทบสูงสุด ข้อสังเกตด้านความปลอดภัย วงจรพึ่งพา และไฟล์ที่ไม่มีใครเรียกใช้
- ส่งออกผลวิเคราะห์ทั้งชุดเป็น JSON ไฟล์เดียวที่ `GET /api/analyses/:id/export`
- เส้นทาง API ใหม่สำหรับข้อสังเกตด้านความปลอดภัยและการจัดอันดับไฟล์ตามมิติที่เลือก

### ปรับ

- โคลนลึกสองร้อยคอมมิตแทนคอมมิตเดียว เพราะจุดร้อนและเจ้าของโค้ดต้องใช้ประวัติ
- สแกนความปลอดภัยกับทุกไฟล์ที่อ่านเป็นข้อความได้ ไม่ใช่เฉพาะไฟล์โค้ด เพราะกุญแจที่หลุดบ่อยที่สุดอยู่ในไฟล์ตั้งค่าและไฟล์สภาพแวดล้อม
- เพิ่มขั้น "คำนวณตัวชี้วัด" เข้าไปในไทม์ไลน์ความคืบหน้า ให้ตรงกับสิ่งที่ระบบทำจริง

## v0.3.0 — เห็นเป็นภาพ

วันที่ 2026-09-07

### เพิ่ม

- แผนที่โค้ดแบบโต้ตอบที่หน้า `/a/[id]` — กราฟความสัมพันธ์ระหว่างไฟล์ที่ลากซูมได้ เรนเดอร์ด้วย WebGL ผ่าน sigma และจัดวางตำแหน่งด้วย ForceAtlas2
- พื้นที่ทำงานสามคอลัมน์ที่แชร์สิ่งที่เลือกอยู่ร่วมกัน — รายการไฟล์ กราฟ และแผงรายละเอียด เลือกจากที่ไหนก็ตาม อีกสองส่วนตามไปด้วยเสมอ
- ต้นไม้โฟลเดอร์ที่คลี่ชั้นบนสุดไว้ให้ตั้งแต่เปิดหน้า และคลี่ตามให้อัตโนมัติเมื่อเลือกไฟล์จากกราฟ
- แผงรายละเอียดของไฟล์ — ฟังก์ชันและคลาสที่ประกาศไว้พร้อมเลขบรรทัด ไฟล์ที่พึ่งพาไฟล์นี้ และไฟล์ที่ไฟล์นี้เรียกใช้ คลิกเพื่อกระโดดต่อได้ทั้งสองทิศทาง
- โหมดลงสีสามแบบ — ตามโฟลเดอร์ ตามภาษา และตามความสำคัญ (จำนวนไฟล์ที่พึ่งพา) พร้อมคำอธิบายสีข้างกราฟและคำใบ้ว่าโหมดนั้นตอบคำถามอะไร
- ค้นหาไฟล์ที่กรองพร้อมกันทั้งรายการและกราฟ ไฟล์ที่ไม่ตรงจะถูกหรี่ลงแทนที่จะหายไป จะได้ยังเห็นว่าอยู่ตรงไหนของโครงสร้าง
- เน้นเพื่อนบ้านเมื่อชี้หรือเลือกโหนด — เส้นและไฟล์ที่ไม่เกี่ยวจะถูกซ่อนชั่วคราว
- เส้นทาง API ใหม่: `GET /api/analyses/:id/graph` สำหรับข้อมูลกราฟ และ `GET /api/analyses/:id/files/detail` สำหรับรายละเอียดของไฟล์เดียว
- ตัดจำนวนโหนดสำหรับ repo ที่ใหญ่เกินวาดไหว โดยเลือกไฟล์ที่มีความสัมพันธ์มากที่สุดก่อน ตัดเส้นที่ปลายอีกข้างไม่ได้ถูกส่งมาด้วย และบอกผู้ใช้ตรง ๆ ว่าตัดมา
- รองรับเครื่องที่เปิด WebGL ไม่ได้ ด้วยข้อความบอกเหตุผลและรายการไฟล์ที่ยังใช้งานได้ตามปกติ

### แก้

- ค้นหาไฟล์แล้วผลลัพธ์ยังถูกซ่อนอยู่ในโฟลเดอร์ที่ปิดอยู่ ทำให้ต้องไล่คลี่เอง ตอนนี้ระหว่างค้นหาจะคลี่โฟลเดอร์ที่มีไฟล์ตรงคำค้นให้ทันที

### ปรับ

- ย้ายหน้าสรุปตัวเลขเดิมไปที่ `/a/[id]/report` แล้วให้ `/a/[id]` เป็นแผนที่โค้ดแทน เพราะสิ่งที่ผู้ใช้อยากเห็นก่อนคือภาพรวมของโครงสร้าง ไม่ใช่ตาราง
- ขยายความกว้างสูงสุดของหน้าเพื่อให้พื้นที่ทำงานสามคอลัมน์อยู่ได้พอดี

## v0.2.0 — ดึงและอ่านโค้ด

วันที่ 2026-09-07

### เพิ่ม

- แพ็กเกจ `@repolens/analyzer` ที่ทำไปป์ไลน์หกขั้นครบ — ตรวจที่อยู่ โคลน สำรวจไฟล์ อ่านโครงสร้าง เชื่อมความสัมพันธ์ และสรุปผล พร้อมรายงานความคืบหน้าออกมาทุกขั้น
- อ่านโครงสร้างโค้ดด้วย tree-sitter (WebAssembly) สำหรับ TypeScript, TSX, JavaScript, Python, Go และ Rust และมีตัวแยกแบบรูปแบบข้อความเป็นตัวสำรองให้อีกกว่ายี่สิบภาษา
- เชื่อมความสัมพันธ์ระหว่างไฟล์ตามกติกาของแต่ละภาษา — เส้นทางสัมพัทธ์, ไฟล์ index, การนำเข้าแบบจุดของ Python, ชื่อโมดูลจาก `go.mod`, นามแฝง `@/` และ `#include` ของ C — พร้อมค่าความมั่นใจกำกับทุกเส้น
- ตัวโคลนที่ปิด symlink ของ git จำกัดเวลา และแปลงข้อความผิดพลาดของ git เป็นเหตุผลภาษาไทยที่ผู้ใช้อ่านรู้เรื่อง
- ตัวสำรวจไฟล์ที่ข้ามโฟลเดอร์ที่ถูกสร้างขึ้น แยกไฟล์ไบนารีออก และยังนับไฟล์ที่ใหญ่เกินอ่านไว้ในผลลัพธ์
- แพ็กเกจ `@repolens/db` — PostgreSQL 17 พร้อมระบบ migration ที่รันซ้ำได้และล็อกกันชนกันเอง เก็บผลวิเคราะห์ ไฟล์ เส้นเชื่อม และ symbol แยกตามคอมมิต
- แอป `@repolens/worker` ที่รับงานผ่านคิว BullMQ + Redis แยกกระบวนการจาก API เพื่อไม่ให้การโคลน repo ไปหน่วงหน้าเว็บ
- เส้นทาง API ใหม่: สั่งวิเคราะห์ ดูสถานะ ดูรายการไฟล์ ดูเส้นเชื่อม และติดตามความคืบหน้าแบบสตรีมด้วย SSE
- แคชผลตามคอมมิตและรุ่นของตัววิเคราะห์ — สั่ง repo เดิมซ้ำจะได้ผลเดิมกลับมาทันทีโดยไม่วิเคราะห์ใหม่
- จำกัดอัตราการสั่งวิเคราะห์ต่อหนึ่งที่อยู่ IP เพราะการโคลนกินทรัพยากรจริง
- หน้าเว็บใหม่: หน้าติดตามความคืบหน้าที่เดินตามขั้นตอนจริงผ่าน SSE และหน้าผลลัพธ์ที่สรุปจำนวนไฟล์ บรรทัดโค้ด เส้นเชื่อม สัดส่วนภาษา ไฟล์ที่ถูกพึ่งพามากที่สุด และแพ็กเกจภายนอกที่ใช้บ่อย
- ชุดทดสอบปลายทางที่วิเคราะห์ repo ตัวอย่างจริงในเครือข่ายของ compose เอง จึงไม่ต้องพึ่งอินเทอร์เน็ต

### แก้

- `/api/health` ตอบ ok ทั้งที่ต่อฐานข้อมูลไม่ได้ — ตอนนี้ยิงคำสั่งเข้าฐานข้อมูลจริงก่อนตอบ compose จึงไม่ประกาศว่าบริการพร้อมทั้งที่ยังใช้งานไม่ได้
- โคลนโฟลเดอร์ที่เจ้าของไฟล์ไม่ตรงกับผู้รันไม่ได้ เพราะ git ปฏิเสธด้วยเหตุผลเรื่องความเป็นเจ้าของ ทั้งที่การโคลนเป็นการอ่านอย่างเดียวและเราไม่รันอะไรจาก repo เลย
- ที่อยู่ในเครื่องที่ไม่มีอยู่จริงให้ข้อความผิดพลาดดิบจากระบบไฟล์ — เปลี่ยนเป็น "ไม่พบโฟลเดอร์ที่ระบุ"

### หมายเหตุอัปเกรด

- รุ่นนี้เพิ่ม PostgreSQL 17 และ Redis 7 เข้ามาใน compose ทั้งสามชุด ต้องตั้ง `POSTGRES_PASSWORD` ใน `.env` ก่อนรันบนเซิร์ฟเวอร์จริง
- migration ถูกรันอัตโนมัติตอน API และ worker เริ่มทำงาน ไม่ต้องสั่งเอง
- เวอร์ชันของ web-tree-sitter ถูกตรึงไว้ที่ 0.20.8 ให้เข้าคู่กับไวยากรณ์ใน tree-sitter-wasms 0.1.13 รุ่นใหม่กว่านั้นเปลี่ยนรูปแบบไฟล์ wasm จนโหลดไวยากรณ์ชุดนี้ไม่ได้

## v0.1.0 — โครงและวินัย

วันที่ 2026-09-07

### เพิ่ม

- โครงสร้าง monorepo ด้วย pnpm workspaces แยกเป็น `@repolens/web`, `@repolens/api` และ `@repolens/shared`
- Fastify API พร้อมเส้นทาง `/api/health`, `/api/version`, `/api/versions`, `/api/versions/:version` และ `/api/session`
- หน้าเว็บ Next.js 15: หน้าแรกที่อธิบายสองระดับการใช้งาน และหน้า `/versions` ที่อ่านบันทึกรุ่นย้อนหลังได้ทุกรุ่น
- ป้ายบอกรุ่นมุมล่างทุกหน้า แสดงเวอร์ชัน วันที่ build และ git sha ที่ฝังตอนสร้างอิมเมจ
- ระบบบันทึกรุ่นภาษาไทย: หนึ่งไฟล์ต่อรุ่นใน `docs/changelog/` ประกอบเป็น `CHANGELOG.md` อัตโนมัติ และเป็นแหล่งข้อมูลเดียวกับที่หน้าเว็บอ่าน
- ตัวตรวจบันทึกรุ่นที่ทำให้ CI ตกถ้าเวอร์ชันในโค้ดกับบันทึกรุ่นไม่ตรงกัน หรือ `CHANGELOG.md` ไม่ถูกประกอบใหม่
- ตารางระดับการใช้งานสองระดับ (ผู้เยี่ยมชม / สมาชิกที่ใส่ API key ของตัวเอง) เป็นแหล่งความจริงเดียวใน `@repolens/shared`
- สภาพแวดล้อม Docker ครบวงจร: `compose.yaml` สำหรับพัฒนา, `compose.test.yaml` สำหรับทดสอบ, `compose.prod.yaml` พร้อม Caddy สำหรับขึ้นจริง และ `Makefile` เป็นทางเข้าเดียวของทุกคำสั่ง
- GitHub Actions ที่รันชุดทดสอบเดียวกับที่นักพัฒนารันในเครื่อง แล้ว build อิมเมจขึ้น registry เมื่อติดแท็กรุ่น
- ระบบสีและตัวอักษรของหน้าเว็บ รองรับทั้งธีมสว่างและมืด พร้อมเคารพการตั้งค่าลดการเคลื่อนไหวของเครื่องผู้ใช้

### แก้

- คำขอ `/api/*` ที่ผ่านหน้าเว็บไปไม่ถึง API เมื่อรันจากอิมเมจ production เพราะ `rewrites` ของ Next ถูกคำนวณตอน build ทำให้ค่า `API_ORIGIN` ถูกฝังตายไปกับอิมเมจ — เปลี่ยนไปใช้ตัวส่งต่อที่อ่านค่าตอนรันจริง

### หมายเหตุอัปเกรด

- รุ่นนี้ยังไม่วิเคราะห์โค้ดจริง การรับลิงก์ repo และไปป์ไลน์วิเคราะห์เริ่มที่ v0.2.0
- ระบบล็อกอินและช่องใส่ API key จะลงจริงพร้อมชั้นความเข้าใจในรุ่น v0.5.0 รุ่นนี้มีเพียงตารางระดับการใช้งานและเส้นทาง `/api/session` ที่รายงานว่าตอนนี้อยู่ระดับผู้เยี่ยมชม
